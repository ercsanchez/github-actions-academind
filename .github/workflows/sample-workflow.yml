name: First Workflow

on: workflow_dispatch

jobs:

  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print something
        run: echo "This is the first step."
      - name: Print another thing
        run: echo "This is the second step."